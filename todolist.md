# RM2027 底盘与云台控制调参待办

## 当前系统链路

### 控制数据链路

- DT7 遥控器的遥控数据通过 DR16 发送给单片机。
- 键盘和鼠标通过 USB 连接电脑，DT7 遥控器采集键鼠输入后，经 DR16 发送给单片机。
- 当前工程为双板架构：
  - `gimbal`：接收 DR16 数据，处理遥控器/键鼠控制，并负责云台控制。
  - `chassis`：负责底盘电机控制；Yaw 轴电机由底盘板控制。
- `gimbal` 板通过 CAN 向 `chassis` 板发送底盘控制数据。
- `gimbal` 板内部通过 CAN 发送云台控制数据，当前涉及 Pitch/Yaw 云台控制链路。

### 图传链路

- 图传发送端直接连接图传接收端。
- 图传接收端连接电脑，为选手端客户端提供图传画面。
- 当前没有使用裁判系统串口与单片机进行通信；后续排查不应将裁判系统串口作为遥控数据链路。

## 当前现象

- 遥控器数据可以发送到底盘，键鼠数据也可以发送到底盘。
- 遥控器数值映射到底盘电机后偏大。
- 键鼠数据映射到底盘电机后偏小。
- 客户端可以看到鼠标移动输入，但目前鼠标无法控制 Pitch 轴运动。
- Pitch 轴电机当前似乎不能稳定正确使能；由于曾经出现过上电后能够锁定的情况，暂时保留硬件故障的可能性，不把它作为当前首要软件问题处理。
- 尚未确认遥控器发送到单片机的数据，以及单片机实际接收到的数据是否一致。

## 已确认的软件问题

- `gimbal/application/cmd/robot_cmd.c` 中的 `MouseKeySet()` 没有设置 `gimbal_cmd_send.gimbal_mode`。
  - 该静态变量默认处于 `GIMBAL_ZERO_FORCE`。
  - 云台任务在该模式下会执行 `DJIMotorStop(pitch_motor)`，因此鼠标 Pitch 指令不会进入正常闭环控制。
- `gimbal/application/robot_def.h` 中 Pitch 软件限位当前为：
  - `PITCH_MAX_ANGLE = 0.8f`
  - `PITCH_MIN_ANGLE = 0.25f`
  - IMU Pitch 使用度数时，这个范围只有约 `0.55` 度，鼠标控制会几乎立即触发限位。
  - 具体范围必须结合机械限位和实机零位确认，不能直接把试验值当作最终值。
- 键盘底盘映射公式存在逻辑错误：
\
- C 键虽然会更新 `chassis_speed_buff`，但 `chassis` 工程当前没有使用该字段进行速度缩放，因此速度档位实际上不生效。
- `chassis/application/chassis/chassis.c` 的 `CHASSIS_ZERO_FORCE` 分支调用 `DJIMotorEnable()`，之后仍继续计算并下发参考值，急停/零力逻辑不正确。

## 风险观察（暂不处理）

以下项目可能影响输入链路的鲁棒性或失控保护，但尚未证实会导致当前功能异常；本阶段仅记录，待基础功能与实机采样确认后再评估。

- [ ] `gimbal/bsp/usart/bsp_usart.c` 的 `HAL_UARTEx_RxEventCallback()` 未校验实际接收长度 `Size`，而 `remote_control` 固定按 18 字节解析 DR16 数据。后续调试串口链路时记录 `Size`，确认每帧均完整后再决定是否增加长度校验。
- [ ] `remote_control` 已提供 `RemoteControlIsOnline()`，但当前 `RobotCMDTask()` 未使用该状态。遥控离线回调会清空原始输入；需在实机验证离线后，底盘和云台是否仍持续发布上一帧控制命令，再决定是否增加失控保护。

## 已确认的映射差异

- 遥控器当前最大速度约为：`30 * 660 = 19800 mm/s`。
- 键盘当前最大速度约为：`300 mm/s`。
- 两条输入链路使用了数量级不同的速度比例，导致遥控器控制过大、键鼠控制过小。
- 底盘 M3508 速度单位换算目前与 `PowerControlInit()` 的 PID 输入单位匹配，暂不修改该部分。

## 待修改事项

- [ ] 在 `MouseKeySet()` 中明确设置键鼠模式：
  - `chassis_mode = CHASSIS_NO_FOLLOW`
  - `gimbal_mode = GIMBAL_GYRO_MODE`
- [ ] 修正键鼠 WSAD 映射，建议使用相互独立的两个轴：
  - `vx = (W - S) * speed`
  - `vy = (D - A) * speed`
  - 正负方向需在实车上确认。
- [ ] 统一遥控器和键鼠的底盘最大速度标定，建议先以约 `3000 mm/s` 作为初始测试上限，再逐步调参。
- [ ] 遥控器输入按摇杆满量程归一化后乘统一最大速度，避免直接使用 `30 * rocker` 的放大方式。
- [ ] 让 `chassis_speed_buff` 真正参与底盘速度缩放，或移除无效档位逻辑后重新定义速度档位。
- [ ] 根据机械限位、IMU 零位和实际安装方向重新设置 Pitch 软件限位。
- [ ] 修正 `CHASSIS_ZERO_FORCE` 的电机处理：停止电机并清零参考值，或在零力模式下直接结束控制流程。
- [ ] 暂不修改 Pitch 使能相关硬件/软件逻辑，先完成输入链路和模式状态确认。

## 实机验证顺序

### 1. 确认输入链路

- [ ] 记录 DT7 摇杆、拨杆、按键、鼠标原始数据。
- [ ] 记录 `gimbal` 单片机从 DR16 解码后的数据，确认遥控器数据是否正确到达。
- [ ] 重点观察 `mouse.x`、`mouse.y`、鼠标按键和键盘状态。
- [ ] 对比客户端显示的鼠标输入、DT7/DR16 输出和单片机接收值，排除输入端缩放或丢包。

### 2. 确认模式和指令

- [ ] 观察 `gimbal_cmd_send.gimbal_mode` 是否为正常云台控制模式，而不是 `GIMBAL_ZERO_FORCE`。
- [ ] 观察 `chassis_cmd_send.chassis_mode` 是否为预期底盘模式。
- [ ] 记录 `gimbal_cmd_send.pitch`、`chassis_cmd_send.vx/vy` 以及进入 CAN 发送前的最终值。
- [ ] 检查 `pitch_motor->stop_flag`、Pitch PID 参考值和反馈值。

### 3. 确认板间通信

- [ ] 抓取/打印 `gimbal` 到 `chassis` 的底盘 CAN 控制帧 `0x312`。
- [ ] 抓取/打印云台控制链路中的 `0x350` 帧，确认 Pitch 指令是否生成并被接收。
- [ ] 在 `chassis` 侧确认 CAN 接收值没有再次被错误放大或缩小。

### 4. 标定控制比例

- [ ] 在遥控器和键鼠分别输入同等比例的前进、后退、左移、右移指令。
- [ ] 记录输入值、板间指令值、电机目标速度和实际速度。
- [ ] 先低速确认方向，再逐步增加比例；确认遥控器和键鼠达到一致的最大速度。
- [ ] 单独确认 `W/S/A/D` 四个方向，确保一个按键只影响对应轴。

### 5. Pitch 专项排查

- [ ] 先确认 `gimbal_mode`、Pitch 目标值和 `stop_flag` 正常，再判断是否存在硬件使能问题。
- [ ] 对比鼠标输入变化时 Pitch PID 参考值是否变化。
- [ ] 检查 IMU `Pitch` 角度和角速度反馈方向、单位及零位。
- [ ] 确认软件限位没有在正常鼠标操作前触发。
- [ ] 最后再检查电机供电、CAN 电机反馈、使能状态、机械阻力和接线。

## 当前约束

- 本阶段只做问题定位和待办记录，不修改源码。
- 未获得实机采样数据前，Pitch 最终限位、方向和速度比例不能定稿。
- 裁判系统串口不属于当前遥控/键鼠控制链路。
